- breadcrumb :payments
= link_to t('shared.add_new_item'), new_payment_path, class: 'btn btn-default'
%table.table.table-condensed
  %thead
    %tr
      %th= t('payment.columns.amount')
      %th= t('payment.columns.student')
      %th= t('payment.columns.completion_date')
      %th= t('shared.actions')
  %tbody
    = render payments
